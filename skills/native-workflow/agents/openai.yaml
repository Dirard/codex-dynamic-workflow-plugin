interface:
  display_name: "Native Workflow"
  short_description: "Запуск workflow, спланированных Codex"
  default_prompt: "Use $codex-dynamic-workflow-plugin:native-workflow to execute this task as a Codex-planned JSON DAG."

dependencies:
  tools:
    - type: "mcp"
      value: "claude-workflow"
      description: "Bundled observable Claude Code Dynamic Workflow lifecycle"
      transport: "stdio"
      command: "node"

policy:
  allow_implicit_invocation: true
